[package]
name = "sequencer_executor_actor_metrics"
version = "0.1.0"
edition = "2024"
license = { workspace = true }

[lints]
workspace = true

[features]
default = []
# Enable metrics record
record = ["dep:metrics"]

[dependencies]
metrics = { workspace = true, optional = true }
