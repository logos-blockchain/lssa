[package]
name = "fee_program"
version = "0.1.0"
edition = "2024"
license = { workspace = true }

[dependencies]
authenticated_transfer_core = { workspace = true, features = ["image_id"] }
fee_core.workspace = true
lee_core.workspace = true
